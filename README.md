# Argus Site

A modern landing page for Argus - a debugging tool for MCP-powered agents. This site showcases Argus's capabilities for capturing, inspecting, and replaying MCP tool calls.

## Features

- 🎨 Modern, responsive design with gradient effects
- 🚀 Built with React 19 and Vite
- 💅 Styled with Tailwind CSS
- 📱 Mobile-first responsive layout
- ⚡ Fast development experience with Vite

## Tech Stack

- **React** ^19.2.3
- **TypeScript** ~5.8.2
- **Vite** ^6.2.0
- **Tailwind CSS** (for styling)

## Getting Started

### Prerequisites

- Node.js (v18 or higher recommended)
- npm or yarn

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd argus-site
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Run the development server:
   ```bash
   npm run dev
   ```

4. Open your browser and navigate to `http://localhost:3000`

## Available Scripts

- `npm run dev` - Start the development server
- `npm run build` - Build the project for production
- `npm run preview` - Preview the production build locally

## TypeScript Configuration

This project includes type declarations for image imports in `vite-env.d.ts`, allowing you to import image files directly in TypeScript components.

## Building for Production

To create a production build:

```bash
npm run build
```

The built files will be in the `dist` directory, ready to be deployed to any static hosting service.

